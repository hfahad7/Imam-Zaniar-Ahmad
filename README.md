# My Site

A simple Next.js website starter.

## Project structure

- `app/layout.js` — root layout shared across pages
- `app/page.js` — the home page content
- `app/globals.css` — global styles

Edit `app/page.js` to change the content, and `app/globals.css` to change the styling.
